import React from 'react';

import { PageWrapper } from '@/components/page-wrapper';

const BlogsPage = () => {
  return (
    <PageWrapper aria-labelledby='blogs-page' className='pt-32'>
      BlogsPage
    </PageWrapper>
  );
};

export default BlogsPage;
